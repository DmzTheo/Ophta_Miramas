<script type="application/ld+json">
{
  "@context": "http://schema.org",
  "@type": "MedicalProcedure",
  "url": "<%= bilancliniquepreoperatoire_url(@bilan) %>",
  "description": "Bilan clinique pré-opératoire - <%= @bilan.name %>"
}
</script>

<!-- BreadcrumbList -->
<script type="application/ld+json">
<% @chirurgies.each do |chirurgie| %>
  <% if chirurgie.nom == @bilan.name %>
    {
      "@context": "https://schema.org",
      "@type": "BreadcrumbList",
        "itemListElement": [{
          "@type": "ListItem",
          "position": 1,
          "name": "chirurgies",
          "item": "<%= chirurgies_url %>"
        },{
          "@type": "ListItem",
          "position": 2,
          "name": "<%= chirurgie.nom %>",
          "item": "<%= chirurgy_url(chirurgie) %>"
        },{
          "@type": "ListItem",
          "position": 3,
          "name": "bilan clinique pré-opératoire",
          "item": "<%= bilancliniquepreoperatoire_url(@bilan) %>"
        }]
    }
  <% end %>
<% end %>
</script>
<!-- End BreadcrumbList -->

<% case @bilan.name %>
  <% when 'Cataracte' %>
    <%= render "cataracte" %>
  <% when 'Glaucome' %>
    <%= render "glaucomechir" %>
  <% when 'IVT' %>
    <%= render "ivt" %>
  <% when 'Chirurgie réfractive' %>
    <%= render "laser" %>
<% end %>

